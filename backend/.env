# НЕ ИСПОЛЬЗУЕТСЯ на Railway! 
# Railway берёт переменные из Variables!
# Этот файл только для локальной разработки!

DB_USER=postgres
DB_HOST=localhost
DB_NAME=corp_messenger
DB_PASSWORD=0000
DB_PORT=5432

PORT=3000
JWT_SECRET=your_super_secret_key
JWT_EXPIRES_IN=7d

CORS_ORIGIN=http://localhost:3001